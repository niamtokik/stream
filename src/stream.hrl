%%%-------------------------------------------------------------------
%%%
%%%-------------------------------------------------------------------
-record(state, { buffer = <<>> :: bitstring()
	       , mode = 1 :: integer()
	       , size = 1
	       , insufficient = undefined :: term()
	       , action = #{} :: map()
	       }).

